THIS IS EXAMPLE APPLICATON
--------------------------

Before using make sure you built 'venv':

    ./buildenv.sh

After that syncronize models by:

    ./manage.py sqla:reset

Now you are ready to start application:

    ./manage.py server:serve

Open 'http://localhost:8000/' in your browser
By default there is user 'admin' with password 'admin'
